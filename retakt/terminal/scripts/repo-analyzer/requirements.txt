# Repo Analyzer Skill Dependencies

# Core dependencies
pyyaml>=6.0
toml>=0.10.2
chardet>=5.2.0

# Development dependencies (optional)
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

# Note: These are the minimum required dependencies
# The skill will work with Python 3.8+ standard library for most operations