Lock YT subdomain to dark mode - light mode styling issues

CRITICAL FIX: Server was serving from /var/www/yt-downloader/dist/ not /frontend/
- Deployment script was uploading to wrong directory
- Fixed by copying frontend/* to dist/

Changes:
- Disabled theme toggle completely (shows console log when clicked)
- Locked YTPage to always use dark mode
- Force clear localStorage and set to "dark" on mount
- Removed theme state management (hardcoded to "dark")
- Added note in UI: "light mode styling issues - locked to dark mode for now"
- Toggle is now disabled, grayed out, and locked to moon icon

TODO: Fix light mode Tailwind issues tomorrow
ROOT CAUSE OF LIGHT MODE BUG:
- Tailwind v4 dark mode not working with class-based approach
- Light mode colors (bg-white, bg-zinc-100) rendering as black
- Even with !important, styles not applying correctly
- Inline styles with document.documentElement.classList.contains('dark') check also failed
- Suspect: Tailwind v4 @variant or @custom-selector not working as expected
- May need to downgrade to Tailwind v3 or use different dark mode strategy

DEPLOYMENT NOTE:
- Server serves from: /var/www/yt-downloader/dist/
- Deploy script should target: dist/ not frontend/
- Update deploy-all.sh and deploy-all.ps1 to use correct path

Files changed:
- yt/YTPage.tsx: Removed theme state, locked to dark, force localStorage
- yt/pages/Settings.tsx: Disabled theme toggle section
- yt/components/ThemeToggle.tsx: Disabled button, added console log, locked to moon
- yt/pages/Home.tsx: Added inline styles (didn't work due to wrong deploy path)
- yt/components/NoticeBox.tsx: Added inline styles (didn't work due to wrong deploy path)
- yt/styles.css: Multiple attempts at Tailwind v4 dark mode config
- deploy-all.ps1: Removed hardcoded CF tokens (use env vars)
- deploy-all.sh: Removed hardcoded CF tokens (use env vars)
